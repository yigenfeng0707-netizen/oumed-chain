fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
# psycopg2-binary==2.9.9  # PostgreSQL 驱动（生产环境按需启用；本地 Demo 使用 SQLite）
redis==5.0.7
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
httpx==0.27.0
chromadb>=0.5.0
openai>=1.35.0
sentence-transformers>=3.0.0
Pillow==10.4.0
python-multipart==0.0.9
aiosqlite==0.20.0
greenlet>=3.0.0            # SQLAlchemy async 必需（Python 3.13 不自动安装）
numpy>=1.26.0
scikit-learn>=1.4.0       # 联邦学习引擎：逻辑回归 + AUC 评估
pandas>=2.0.0             # 联邦学习引擎：合成医疗数据集
pypdf>=4.0.0             # 档案管家：PDF 报告文本提取

# EEG 真实设备接入（可选，按需安装；Demo/比赛环境可跳过）
# Muse 头环：pip install pylsl muselsl bleak
# EDF 文件：pip install pyedflib
# pylsl>=1.16.0          # LSL 协议（Muse/Emotiv/OpenBCI 实时流）
# pyedflib>=0.1.38       # EDF 文件解析（临床标准格式）
